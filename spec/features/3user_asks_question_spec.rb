require "spec_helper"

feature "user attempts to ask a new question" do
  scenario "submits a question with all requirements met"

  scenario "submits a title shorter than 50 characters"

  scenario "submits a description shorter than 150 characters"
end
